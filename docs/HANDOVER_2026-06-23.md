# Handover — start here (2026-06-23)

Short, current-state handover. For deep history see `docs/STATUS_NOTES.md`,
`docs/PUBLISHING_PLAN.md`, and `git log`.

## 1. Worktrees (who owns what)
`git worktree list`:
- **`/Users/victoriakofman/CLAUDE CODE/Domino`** — branch **`work/cardmaker-rowcopy`** —
  the **MAIN tree**. Big Game / Previewer / **Publishing** session. Owns
  `index.html`, `biggame.html`, `js/game.js`, `js/sync.js`, `css/style.css`,
  `gallery.html`, `firebase*`, `.github/`.
- **`/Users/victoriakofman/CLAUDE CODE/Domino-studio`** — branch **`work/studio`** —
  the **STUDIO session** (Card Maker / Game Creator / IC). Owns **`pm-studio-DrV.html`**.
  Runs CONCURRENTLY; its commits interleave in the main tree's log via ff-merge.
- `.claude/worktrees/upbeat-nightingale-d230e4` — a stray agent worktree; ignorable
  / safe to prune (`git worktree remove`).

## 2. Branch + ship/commit agreement
- Work on **`work/cardmaker-rowcopy`** (main tree). Do NOT use `main`/`origin/main` (stale).
- **Canonical = 3 mirror branches** kept byte-identical:
  `claude/review-project-docs-JOOeh` (primary) + `claude/general-session-yVBQq` +
  `claude/resume-vica-domin-UOJun`.
- **Ship with `bash scripts/ship.sh "msg"`** — bumps the banner, commits, and pushes
  HEAD to all 3 mirrors with ✓/✗ per branch. Use **per-path** form when a concurrent
  session is live so you don't sweep its files: `ship.sh "msg" -- index.html js docs`.
- **Commit/push only when the user asks.** (This session the user said "ship it"
  repeatedly while iterating live, so we shipped each increment.)
- **⚠ Before working: `git fetch` + check `git log`/`git status`.** The main tree may
  be AHEAD of the canonical origin tips because the Studio session committed locally
  (e.g. at handover, local HEAD `43b2c28` had 3 Studio commits above the last pushed
  publishing tip `61deb72`). Reconcile/push state before assuming you're in sync.
- **Cache-busters are manual** in the HTML `<script>`/`<link>` query strings — bump
  them when you change `js/*.js` or `css/*`. Latest seen: `sync.js?v=local-wins-15`,
  `firebase-config.js?v=2` (verify in-file before bumping).

## 3. Deploy = automatic (no Cloud Shell)
- A **GitHub Action** (`.github/workflows/firebase-hosting-deploy.yml`) deploys to
  **Firebase Hosting** (project `vica-domino`) on every push to
  `claude/review-project-docs-JOOeh`. So **`ship.sh` → live on mathgrain.com** in ~40s.
- Requires repo secret **`FIREBASE_SERVICE_ACCOUNT_VICA_DOMINO`** (already set).
- Manual fallback (Cloud Shell, browser): `git clone … && git checkout
  claude/review-project-docs-JOOeh && firebase deploy --only hosting --project vica-domino`.

## 4. Current state — PUBLISHING is LIVE & SECURED
Built + deployed this session (see `docs/PUBLISHING_PLAN.md`):
- **Publish a named mini-game** → frozen self-contained bundle (`_pubBuildBundle`,
  art baked into svgMarkup) → cloud `published/<id>` (`syncPublish*` in sync.js) via
  the **Pub** button on named mini-game rows (publish / re-publish / unpublish / copy-link).
- **Tester gallery** `gallery.html` (Firebase email/pw login → grid → play).
- **Published player** `index.html?playPublished=<id>` (frozen art via `_pubPlayMode`,
  contained — all editor-exit paths incl. `game.resetToSetup` → gallery).
- **Hosting:** mathgrain.com (GoDaddy DNS → Firebase, SSL). gallery is the tester door.
- **SECURITY — editor locked to the owner:**
  - **Gate** (`_editorGate` in index.html; same gate in biggame.html) — deployed-only,
    requires Firebase sign-in as a **`SUPERUSER_EMAILS`** (firebase-config.js =
    `victor49@gmail.com`); non-owners → gallery. **localhost EXEMPT** (dev),
    **`?playPublished` EXEMPT** (testers).
  - **Firestore rules:** `published/*` read=any-authed, write=victor49;
    **`users/**` read+write = victor49 only** (closes the old name-based "Vica" hole).
  - **Superuser cloud-sync gated on Firebase victor49** (`_canSuperuserSync` in sync.js)
    so the name-based session never throws permission-denied; tester surfaces set
    `window._syncSuppressSuperuser` / `_syncSuppressUnloadGuard`.
  - **⚠ Workflow:** any device EDITING content must be Firebase-signed-in as victor49
    to sync to cloud. Deployed editor gate handles it; **localhost is gate-exempt, so
    local edits stay in localStorage and only push to cloud if you firebase-sign-in
    as victor49 there** (sign in once). Testers/published unaffected.
- **Tester accounts (Firebase Auth):** victor49@ (owner), drkofman@, lianacalc@gmail.com.
  Login failures = wrong account password (reset in Auth → Users), not a bug; gallery
  and the play link share the same accounts.

## 5. Concurrent STUDIO session (separate, do not collide)
`../Domino-studio` on `work/studio` is actively editing **`pm-studio-DrV.html`**
(latest: Card Maker "Par" math-params, set-icons via Add-Cards). It ships per-path
and ff-merges into the main tree. Keep publishing/editor edits to index/biggame/sync/
game/css; leave pm-studio-DrV.html to that session.

## 6. Open follow-ups (ranked, all optional)
1. **Root → gallery for non-owners** — mathgrain.com root shows the editor login;
   make non-owners land on the gallery instead (tester-friendlier front door).
2. **Masked password modals** — publish/editor/gallery sign-in use plain inputs/prompt;
   polish to a proper modal.
3. **Transitions library** (original Q1) — Studio authors reusable transitions; Big-Game
   session assigns/plays. Store as condition→transition RULES (round-count = first
   condition). The Big-Game `transition` slot already exists (biggame.html).
4. **Advanced / telemetry publishing** — who played, counts, time, which problems/roles
   were hard → tune (manual first, auto later). Bundle already stamps publishId/version/
   publishedAt for this.
5. **Big Games publishing** (freeze stages + transitions) — later, harder than mini-games.
